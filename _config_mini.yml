# 最小化测试配置
title: "测试网站"
theme: minimal-mistakes-jekyll
minimal_mistakes_skin: "dark"

# 关键配置
include:
  - _pages

defaults:
  - scope:
      path: "_pages"
      type: pages
    values:
      layout: single
      author_profile: true

# 禁用所有插件,避免依赖问题
plugins: []